# Asterra Fleur — Render setup

1. Create a **Static Site** in Render.
2. Root Directory: leave empty.
3. Build Command: leave empty.
4. Publish Directory: `.`
5. Deploy the latest commit.

## Supabase admin login

- Username: `varshuzz`
- Admin email: `VarshaNarendrakumar68@gmail.com`
- Password: set/reset this in Supabase Authentication → Users.
- Supabase URL must be the project URL for the same project as the publishable key.

If login says **Cannot reach Supabase**, the browser cannot reach the configured Supabase endpoint/key pair. Do not put a secret/service-role key in frontend code.
